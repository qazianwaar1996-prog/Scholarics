{
  "name": "scholarics",
  "version": "2.0.0",
  "description": "Scholarics — Academic Tools & AI (Cloudflare Pages + Functions)",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2024-11-01",
    "deploy": "wrangler pages deploy .",
    "test": "wrangler pages dev . --compatibility-date=2024-11-01 --port 8788"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  },
  "engines": {
    "node": ">=18"
  }
}
